import React from 'react'

import { cn } from '../common'

export const CandidateContact = ({ children }) => <div className={cn('candidate-contact')}>{children}</div>

export default CandidateContact
