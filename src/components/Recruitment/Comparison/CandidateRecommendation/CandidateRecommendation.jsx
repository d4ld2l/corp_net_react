import React from 'react'
import { cn } from '../common'

export const CandidateRecommendation = ({ children }) => (
  <div className={cn('candidate-recommendation')}>{children}</div>
)

export default CandidateRecommendation
